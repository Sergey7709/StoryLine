import { Button, Image } from "@mantine/core";
import React from "react";
import styles from "./logoStore.module.css";

export const LogoStore = () => {
  return (
    // <Button h={50} className={styles.btn}>
    <Button h={50} w={120} className={styles.button}>
      МИР КНИГ
    </Button>
  );
};
